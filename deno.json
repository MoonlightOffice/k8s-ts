{
  "tasks": {
    "emit": "TASK=emit deno run -A task.ts",
    "emit-secret": "TASK=emit-secret deno run -A task.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/yaml": "jsr:@std/yaml@^1.0.6",
    "$pkg": "./pkg/index.ts",
    "$config": "./config/index.ts",
    "$files": "./files/index.ts"
  }
}
